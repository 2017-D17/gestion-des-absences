import { CongesJourCollabFilterPipe } from './conges-jour-collab-filter.pipe';

describe('CongesJourCollabFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CongesJourCollabFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
