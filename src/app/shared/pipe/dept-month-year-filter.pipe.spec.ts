import { DeptMonthYearFilterPipe } from './dept-month-year-filter.pipe';

describe('DeptMonthYearFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DeptMonthYearFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
